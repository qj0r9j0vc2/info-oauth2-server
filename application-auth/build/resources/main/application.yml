
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/info_prod
    username: root
    password: info-database-root-user
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: 9000

logging:
  level:
    org:
      springframework:
        security: DEBUG

mail:
  host: smtp.naver.com
  port: 587
  username: info-dsm@naver.com
  password: qj0r9j0vc2
  protocol: smtp
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
